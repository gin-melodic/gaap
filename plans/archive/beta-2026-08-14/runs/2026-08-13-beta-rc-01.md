# UAT-20260813-BETA-RC-01 — Beta RC Full Closure

- Execution date: 2026-08-13 (Asia/Shanghai)
- Environment: `https://gaap.local`, production-mode UAT Compose
- Data prefix: `uat-20260813-*`
- Baseline: root repo `91e8e14`, API `667d7f9`, Web `54f0d24`
- Data protection: all Docker volumes preserved; an AES-256-CBC/PBKDF2 encrypted PostgreSQL backup was created before any writes and its decrypt-and-readability was verified. Backup keys and credentials do not enter the repository or evidence files.
- Final result: **82/82 Beta cases PASS, 0 FAIL, 0 NOT RUN; 38 DEFERRED remain deferred.**

## Statistics Calibration

Counting the six Beta module files one by one gives Authentication 15, Accounts 19, Transactions 27, Dashboard/Settings/User 7, Health Checks 3 and Security/Integrity/Concurrency 11 — 82 items in total. Pre-run status was 13 PASS and 69 NOT RUN, matching the README's 82/69. The earlier 80/67 counting basis had missed two security token cases
`TC-EDGE-SEC-004` and `TC-EDGE-SEC-005`; the split files from the original workbook had no missing cases, so no test items were added or removed.

## Protocol UAT

Using the protobuf, ALE and `secureRequest` chain from the Web production code, 76 formal cases plus one "account with transactions refuses deletion" extended check were executed over HTTPS. The final clean batch was all PASS, covering:

- Registration, login, refresh rotation, failure paths, length/Unicode/special-character boundaries;
- Five concurrent independent sessions, each refreshing separately, and another session still usable after logging out one;
- Account CRUD, filtering, counts, amount boundaries, cross-user access and Free-tier group/sub-account bypass;
- Transaction CRUD, filter/sort/pagination, income/expense/transfer, amount/date/note boundaries;
- 10-way concurrent account creation, concurrent updates of the same transaction, stored SQL injection and XSS payloads;
- Empty-data and populated Dashboard, monthly statistics, and trend convergence after creating, date-moving or deleting historical transactions;
- Settings and current user profile.

The raw ALE security runner executed 7 additional scenarios, all PASS: full replay, signature tampering, ciphertext tampering, expired timestamp, random-signed sid, expired access token, invalid token. Evidence output contains only case IDs, status, HTTP assertions and masked summaries; no passwords, JWTs, cookies, session keys or ALE/JWT secrets are recorded. Batch logs for API, Web and Caddy were scanned with counting-only sensitive pattern matching: password assignments, Bearer JWTs, session
keys, ALE/JWT secrets, decrypted payloads and request/response body patterns all matched 0 times, and no potentially sensitive lines were printed.

## Browser UAT

Login, Dashboard, account list, account create/delete, transaction create/update/
delete, refresh, logged-in page access after Redis restart and logout were executed on real Chrome pages. XSS notes render as text, no injected script node appears in the DOM, and the payload never executed. The browser run found that the Dashboard formatted CNY balances as USD before the profile loaded; after the fix, the UAT Web image was rebuilt and retested to show CNY with no page errors.

## Fault Recovery & Health Checks

- live/ready, PostgreSQL, Redis, RabbitMQ and migration status all normal.
- `gaap.dashboard` and `gaap.tasks` each had 1 consumer and 0 backlog messages.
- After a Redis restart, ready stayed 200; the persistent session remained usable by design.
- During a RabbitMQ outage ready was 503; after recovery no API restart was needed, ready returned to 200 within 17 seconds, and both consumers recovered automatically.
- After an API restart, ready recovered to 200 with all consumers intact; account balances were not rewritten by the startup process.

## Ledger & Automation Evidence

Final `REPEATABLE READ, READ ONLY` reconciliation: 142 accounts and 62 transactions with
`passed=true`, `differences=[]` and `issues=[]`. API `go test ./...` passed all 152 tests and
`go build ./...` passed; Web Vitest passed its 74 standard tests, and ESLint plus `tsc --noEmit` passed.
The Next.js production build for the production Web image passed. A full gate run plus building of the final API/Web candidate images will still be performed before the release commit.

## Defects in This Batch

- The amount database column `NUMERIC(20,9)` could not store the maximum amount allowed by the API: it was widened to
  `NUMERIC(28,9)`, with a migration and decimal boundary tests added; UAT retest with large amounts PASS.
- Over-length account names / transaction notes previously fell through to database errors and triggered session resyncs: Unicode rune boundary validation and tests were added; UAT retest PASS.
- A Dashboard profile-loading race displayed CNY data as USD and threw a currency mismatch: the real account/transaction currency is now used as the basis during loading, Decimal.js tests were added, and the browser retest PASSed.

Local UAT gate conclusion: **PASS**. Public release still depends on GitHub candidate artifacts and pre-production gates such as the VPS/real DNS, Turnstile, Secrets and backup restore.
