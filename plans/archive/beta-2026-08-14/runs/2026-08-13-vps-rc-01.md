# UAT-20260813-VPS-RC-01 — VPS Production Candidate Verification

- Execution date: 2026-08-13
- Target: `gin@144.34.237.205:2021`, `/opt/gaap`
- Architecture: `linux/amd64`
- Conclusion: **Container layer PASS; public network layer BLOCKED; overall NO-GO**

## Candidate Artifacts

- API: `2a163356eca1b5edb6b66a87a467aadeddf37dcb`
- Web: `6862ca2825b0b2da86760ada4a84507f46c47eaf`
- Root infrastructure: `826b5cf46587554526710cc83f79b3045ac2f9b2`
- API digest: `sha256:7e7546fef26de2da228c13b9db4658dddc1cf439d6b93e1773dcfc3ac75e5be8`
- Web digest: `sha256:4c3ef686d0085c854695f2fb1ba74a7b06010e641a888c4f362ed32c7191e3fc`

Both OCI indexes contain a `linux/amd64` manifest. VPS Compose uses `image@sha256:...` references and does not use `latest`; GHCR login was used for pulling only, and logout was performed afterwards.

## Containers, Migrations & Health

- PostgreSQL 18, Redis 7, RabbitMQ 4.1.8, API and Web were started in fresh named volumes; no
  UAT data imported and no existing VPS containers or data deleted or overwritten.
- API, Web, PostgreSQL, Redis and RabbitMQ all healthy.
- API live returned `{"status":"alive"}` and ready returned `{"status":"ready"}`; Web
  `/login` returned 200.
- `schema_migrations` records 5 migrations.
- RabbitMQ `gaap.dashboard` and `gaap.tasks` each had 1 consumer and 0 backlog messages.

## Fault Recovery & Reconciliation

- RabbitMQ restart: first observed ready was 503, recovered to 200 after 14 seconds; both consumers auto-recovered to 1 with no message backlog.
- Redis restart: ready stayed 200 with a 0-second recovery window.
- API restart: first connection status was 000, ready recovered to 200 after 1 second.
- `REPEATABLE READ, READ ONLY` reconciliation run before and after restarts: `passed=true`, checked 0 accounts and
  0 transactions with `differences=[]` and `issues=[]`. An empty-database result only proves the migrations and read-only reconciler can run; it does not replace a real business smoke.

## Public Network Blocker Evidence

- Cloudflare DoH: public `gaap.cc` resolves to a Cloudflare-proxied address; `www.gaap.cc` returns
  NXDOMAIN.
- `https://gaap.cc/login` returned a JSON 404 from the old service, not the new Web app.
- Direct TLS handshake for `gaap.cc` on `144.34.237.205:443` failed; Caddy has no certificate for that domain yet.
- The existing Cloudflare API token on the VPS queried `zones?name=gaap.cc`, which succeeded but returned an empty zone list — i.e., the token lacks `gaap.cc` zone permission, so DNS-01 certificate issuance and record verification/modification are impossible.

Clearing the blocker requires granting that token Zone DNS edit permission for `gaap.cc` and pointing the apex to
`144.34.237.205` plus creating a `www` record. Afterwards, HTTP→HTTPS, certificate chain, security headers, real Turnstile whitelisted registration and the full production business smoke must be run.

## Deliberate Deferrals

The release owner decided not to take a production database backup this round. Encrypted backups, restore into an independent empty DB, the daily job and 7-day retention are all recorded as **DEFERRED** and were not counted as PASS; per the main release gate, the final conclusion remains **NO-GO**.
