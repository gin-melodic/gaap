# UAT-20260813-VPS-RC-01 — VPS 生产候选验证

- 执行日期：2026-08-13
- 目标：`gin@144.34.237.205:2021`，`/opt/gaap`
- 架构：`linux/amd64`
- 结论：**容器层 PASS；公网层 BLOCKED；整体 NO-GO**

## 候选产物

- API：`2a163356eca1b5edb6b66a87a467aadeddf37dcb`
- Web：`6862ca2825b0b2da86760ada4a84507f46c47eaf`
- 根基础设施：`826b5cf46587554526710cc83f79b3045ac2f9b2`
- API digest：`sha256:7e7546fef26de2da228c13b9db4658dddc1cf439d6b93e1773dcfc3ac75e5be8`
- Web digest：`sha256:4c3ef686d0085c854695f2fb1ba74a7b06010e641a888c4f362ed32c7191e3fc`

两个 OCI index 均包含 `linux/amd64` manifest。VPS Compose 使用 `image@sha256:...`，
未使用 `latest`；GHCR 登录仅用于拉取，拉取后已执行 logout。

## 容器、迁移与健康

- 在全新命名 volume 中启动 PostgreSQL 18、Redis 7、RabbitMQ 4.1.8、API、Web；未导入
  UAT 数据，未删除或覆盖 VPS 既有容器与数据。
- API、Web、PostgreSQL、Redis、RabbitMQ 全部 healthy。
- API live 返回 `{"status":"alive"}`，ready 返回 `{"status":"ready"}`；Web
  `/login` 返回 200。
- `schema_migrations` 记录 5 个迁移。
- RabbitMQ `gaap.dashboard`、`gaap.tasks` 均为 1 个消费者、0 条积压。

## 故障恢复与对账

- RabbitMQ 重启：ready 首次观测为 503，14 秒恢复 200；两个消费者自动恢复为 1，
  无消息积压。
- Redis 重启：ready 保持 200，0 秒恢复窗口。
- API 重启：首次连接状态为 000，1 秒恢复 ready 200。
- 重启前后运行 `REPEATABLE READ, READ ONLY` 对账：`passed=true`，检查 0 个账户、
  0 笔交易，`differences=[]`、`issues=[]`。空库结果只证明迁移和只读对账器可运行，
  不替代真实业务 smoke。

## 公网阻断证据

- Cloudflare DoH：公开 `gaap.cc` 为 Cloudflare proxied 地址；`www.gaap.cc` 返回
  NXDOMAIN。
- `https://gaap.cc/login` 返回旧服务的 JSON 404，不是新 Web。
- 直连 `144.34.237.205:443` 的 `gaap.cc` TLS 握手失败，Caddy 尚无该域名证书。
- VPS 现有 Cloudflare API token 查询 `zones?name=gaap.cc` 返回成功但 zone 列表为空，
  即该 token 没有 `gaap.cc` zone 权限，无法完成 DNS-01 证书签发或核对/修改记录。

解除阻断需为该 token 授予 `gaap.cc` Zone DNS 编辑权限，并把 apex 指向
`144.34.237.205`、创建 `www` 记录。之后必须补跑 HTTP→HTTPS、证书链、安全头、真实
Turnstile 白名单注册及完整生产业务 smoke。

## 主动延期

发布负责人决定本轮不做生产数据库备份。加密备份、独立空库恢复、每日任务和 7 天保留
均登记为 **DEFERRED**，没有记为 PASS；根据主发布门禁，最终结论保持 **NO-GO**。
