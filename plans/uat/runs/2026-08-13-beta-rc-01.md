# UAT-20260813-BETA-RC-01 — Beta RC 全量收口

- 执行日期：2026-08-13（Asia/Shanghai）
- 环境：`https://gaap.local`，production-mode UAT Compose
- 数据前缀：`uat-20260813-*`
- 基线：根仓库 `91e8e14`，API `667d7f9`，Web `54f0d24`
- 数据保护：保留全部 Docker volumes；写入前已创建 AES-256-CBC/PBKDF2 加密的
  PostgreSQL 备份并完成解密可读性验证。备份密钥和凭据不进入仓库或证据。
- 最终结果：**82/82 Beta 用例 PASS，0 FAIL，0 NOT RUN；38 DEFERRED 保持延期。**

## 统计校准

对六个 Beta 模块文件逐一统计，实际为认证 15、账户 19、交易 27、Dashboard/配置/用户
7、健康检查 3、安全/完整性/并发 11，共 82 项。执行前状态为 13 PASS、69 NOT RUN，
与 README 的 82/69 一致。先前的 80/67 口径遗漏了两个安全 token 用例
`TC-EDGE-SEC-004`、`TC-EDGE-SEC-005`；原工作簿拆分文件没有缺失用例，因此未新增或
删除测试项。

## 协议 UAT

使用 Web 生产代码中的 protobuf、ALE 和 `secureRequest` 链路，通过 HTTPS 执行 76 个
正式用例及 1 个“有交易账户拒绝删除”扩展检查。最终干净批次全部 PASS，覆盖：

- 注册、登录、refresh 轮换、失败路径、长度/Unicode/特殊字符边界；
- 五个并发独立 session、分别 refresh、退出一个 session 后另一个仍可用；
- 账户 CRUD、筛选、计数、金额边界、跨用户访问和 Free 等级组/子账户绕过；
- 交易 CRUD、筛选/排序/分页、收入/支出/转账、金额/日期/备注边界；
- 10 路并发开户、同一交易并发更新、SQL 注入与 XSS 载荷存储；
- 空数据与有数据 Dashboard、月度统计，以及历史交易创建、移动日期、删除后的趋势收敛；
- 配置与当前用户资料。

原始 ALE 安全 runner 另执行 7 个场景并全部 PASS：完全重放、签名篡改、密文篡改、
过期 timestamp、随机签名 sid、过期 access token、无效 token。证据输出仅含用例 ID、
状态、HTTP 断言和脱敏摘要，不记录密码、JWT、Cookie、session key 或 ALE/JWT secret。
对 API、Web、Caddy 本批次日志只做计数式敏感模式扫描，密码赋值、Bearer JWT、session
key、ALE/JWT secret、解密载荷和请求/响应 body 模式匹配数均为 0，未输出潜在敏感行。

## 浏览器 UAT

使用真实 Chrome 页面执行登录、Dashboard、账户列表、账户创建/删除、交易创建/更新/
删除、刷新、Redis 重启后的已登录页面访问、退出。XSS 备注以文字显示，DOM 中未出现
注入脚本节点，载荷未执行。浏览器发现 Dashboard 在 profile 加载前以 USD 格式化 CNY
余额，修复后重建 UAT Web 镜像并复测为 CNY，页面无错误。

## 故障恢复与健康检查

- live/ready、PostgreSQL、Redis、RabbitMQ 和迁移状态正常。
- `gaap.dashboard`、`gaap.tasks` 均为 1 个消费者、0 条积压。
- Redis 重启后 ready 保持 200；持久化 session 继续按设计可用。
- RabbitMQ 中断时 ready 为 503；恢复后 API 无需重启，17 秒内 ready 回到 200，两个
  消费者均自动恢复。
- API 重启后 ready 恢复 200，消费者完整；账户余额未被启动流程改写。

## 账务与自动化证据

最终运行 `REPEATABLE READ, READ ONLY` 对账：142 个账户、62 笔交易，
`passed=true`、`differences=[]`、`issues=[]`。API `go test ./...` 为 152 个测试通过，
`go build ./...` 通过；Web Vitest 74 个常规测试通过、ESLint 与 `tsc --noEmit` 通过。
生产 Web 镜像的 Next.js production build 通过。发布提交前仍会再次执行完整门禁并构建
最终 API/Web 候选镜像。

## 本批次缺陷

- 金额数据库列 `NUMERIC(20,9)` 无法保存 API 允许的金额上界：已扩展为
  `NUMERIC(28,9)`，补迁移与 decimal 边界测试，UAT 大金额复测 PASS。
- 账户名称/交易备注超限曾落到数据库错误并触发 session 重同步：已增加 Unicode rune
  边界校验和测试，UAT 复测 PASS。
- Dashboard profile 加载竞态导致 CNY 数据按 USD 显示并抛出币种不匹配：已以真实账户/
  交易币种作为加载期依据，补 Decimal.js 测试，浏览器复测 PASS。

本地 UAT 门禁结论：**PASS**。公网发布仍依赖 GitHub 候选产物和 VPS/真实 DNS、
Turnstile、Secret、备份恢复等预生产门禁。
