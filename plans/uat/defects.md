# Beta Defect List

This file merges the Bug and TODO sheets of the original workbook and adds blocking issues confirmed by source-code audit.

| ID | Severity | Module | Description | Status | Verification Case | Fix Commit |
|---|---|---|---|---|---|---|
| DEF-001 | P0 | Account/Currency | New account defaults to USD, but the settings page shows base currency CNY | RESOLVED / UAT PASS | TC-ACCT-CREATE-001 | pending commit on release branch |
| DEF-002 | P0 | Account summary | Parent-account amounts not displayed in base currency with wrong sign | DEFERRED / PRO FEATURE | deferred account-group regression | pending commit on release branch |
| DEF-003 | P1 | Settings/FX rate | FX rate update fires two toasts | DEFERRED / UI HIDDEN | deferred feature | pending commit on release branch |
| DEF-004 | P0 | Account creation | New account and parent/child accounts do not uniformly use the user's base currency | RESOLVED / BETA UAT PASS + GROUP DEFERRED | TC-ACCT-CREATE-001; TC-ACCT-CREATE-004 deferred | pending commit on release branch |
| DEF-005 | P1 | Account creation | Creating a new account fires multiple toasts | IMPLEMENTED / TESTED | TC-ACCT-CREATE-001 | pending commit on release branch |
| DEF-006 | P0 | ALE | Logs print keys and decrypted request/response payloads | RESOLVED / UAT LOG SCAN PASS | security log scan | pending commit on release branch |
| DEF-007 | P0 | Transaction authorization | Transactions could reference another user's accounts and modify their balances | RESOLVED / UAT PASS | TC-EDGE-SEC-003 | pending commit on release branch |
| DEF-008 | P0 | Transaction query | sort_by was not whitelisted, posing a SQL injection risk | IMPLEMENTED / TESTED | TC-EDGE-SEC-001 | pending commit on release branch |
| DEF-009 | P0 | Transaction transactionality | The failed path of transaction creation may not roll back; commit errors were ignored | RESOLVED / FAILURE-INJECTION + UAT PASS | TC-EDGE-DATA-001 | 316bc88 + pending workspace commits |
| DEF-010 | P0 | Protocol | Frontend JSON and ALE+PB request stacks coexisted | IMPLEMENTED / SMOKE TESTED | protocol integration tests | pending commit on release branch |
| DEF-011 | P0 | Amounts | Frontend Dashboard/account summaries used number types with hardcoded FX rates | IMPLEMENTED / TESTED | amount boundary tests | pending commit on release branch |
| DEF-012 | P0 | Deployment | Production Compose, migration versions and availability probes were incomplete | PARTIAL / VPS CONTAINERS PASS / PUBLIC DNS BLOCKED | deployment smoke test | `826b5cf` |
| DEF-013 | P0 | Auth | Login Turnstile was not validated server-side; browser pre-hashing let weak passwords bypass UI validation, and 6-character passwords were submittable while only a generic failure was shown | RESOLVED / FULL AUTH UAT PASS | TC-AUTH-REG-003 to 005, TC-AUTH-LOGIN-001 to 003 | pending workspace commits |
| DEF-014 | P0 | Transaction/instant account creation | After instantly creating a spending account, the returned account type was not used; asset→spending was wrongly submitted as a transfer and an orphan account was left behind | RESOLVED / UAT PASS | TC-TXN-CREATE-002, instant-spending-account extended scenario | pending commit on release branch |
| DEF-015 | P0 | ALE/transaction delete | When the Redis session key was missing, no recognizable 401 was produced and the frontend exposed a raw `OperationError`; the delete request never actually reached the controller | RESOLVED / UAT PASS | transaction delete, post-Redis-restart session, extended scenario for session key sync recovery | pending commit on release branch |
| DEF-016 | P0 | Dashboard/RabbitMQ | Production mode skipped RabbitMQ initialization; after a transaction only the Redis snapshot was cleared and then stale snapshots were restored from PostgreSQL, so historical-date transactions never entered trend charts | RESOLVED / UAT PASS | trend chart after creating/updating/deleting historical-date transactions; RabbitMQ/ready | 316bc88 + pending workspace commits |
| DEF-017 | P0 | Startup/balances | The startup balance recalculation queried integer enums with strings and skipped failed accounts; rules are incomplete, unsuitable as a production auto-repairer | MITIGATED / UAT RECONCILIATION PASS | balances unchanged across container restarts; read-only account-balance vs transaction-ledger reconciliation | 316bc88 + pending workspace commits |
| DEF-018 | P1 | Account delete | The backend already forbids migration-delete, but the frontend still showed a migration target picker implying the task is executable | RESOLVED / UAT PASS | delete account with transactions; delete account without transactions | pending commit on release branch |
| DEF-019 | P0 | Account authorization | The account-group UI is Pro-only, but the create-account service did not validate user tier yet; Free/Beta users could bypass the UI to submit `is_group` or `parent_id` | RESOLVED / UAT PASS | TC-EDGE-SEC-003; TC-ACCT-CREATE-003/004 remain deferred | pending workspace commits |
| DEF-020 | P0 | RabbitMQ/availability | After a brief RabbitMQ outage the container returned to healthy, but the API client did not auto-reconnect, `ready` stayed at 503 and only an API restart recovered it | RESOLVED / UAT PASS | TC-HEALTH-001; RabbitMQ recovery test | pending workspace commits |
| DEF-021 | P1 | Auth/email boundary | The registration email field had no max-length limit; a 262-character email could be submitted and the page only showed a generic failure instead of a length-exceeded notice | RESOLVED / UAT PASS | TC-EDGE-AUTH-001 | pending workspace commits |
| DEF-022 | P0 | Amounts/database | The maximum amount allowed by the API exceeded `NUMERIC(20,9)`; large accounts and transactions overflowed in PostgreSQL | RESOLVED / UAT PASS | TC-EDGE-ACCT-001, TC-TXN-CREATE-010 | `2a163356` |
| DEF-023 | P1 | Input boundary | Over-length account names and transaction notes fell through to database errors and triggered unnecessary session resyncs | RESOLVED / UAT PASS | TC-EDGE-ACCT-002, TC-EDGE-TXN-003 | `2a163356` |
| DEF-024 | P1 | Dashboard/currency | Before the profile loaded, the Dashboard formatted CNY balances as USD and threw a currency mismatch | RESOLVED / BROWSER UAT PASS | TC-DASH-SUMMARY-001, TC-DASH-TREND-001 | `4937d1db` |
| DEF-025 | P2 | Dashboard/charts | During production Dashboard initialization and reloads, chart containers briefly emit `width(-1)` / `height(-1)` console warnings; charts render normally afterwards | OPEN / NON-BLOCKING | 2026-08-14 production Dashboard smoke | pending fix |
| DEF-026 | P2 | UI/accessibility | New-account and new/edit/delete transaction dialogs lack `Description` or `aria-describedby`, causing persistent browser console warnings | OPEN / NON-BLOCKING | 2026-08-14 production account/transaction smoke | pending fix |
| DEF-027 | P2 | Transactions/date | The new-transaction UI labels the field `Transaction Time` and accepts `datetime-local`, but the API only returns `YYYY-MM-DD`; the frontend parses that date as UTC midnight, so in the Shanghai timezone list/edit forms uniformly display 08:00 and user-entered hours/minutes/seconds are lost | OPEN / NON-BLOCKING FOR DATE-BASED BETA | 2026-08-14 production transaction smoke | pending decision between date-only or end-to-end time retention |
| DEF-028 | P1 | ALE/error channel | Every server validation error on the secure session channel surfaces as a generic `Unable to verify secure API response` toast instead of the real message. Reproduced for two distinct 400 causes (cross-currency transfer → `account currency mismatch`; same-account transfer → `source and destination accounts must differ`). Server logs show proper HTTP 400 with an encrypted ErrorResponse written under the valid session key; client-side `decryptPayload` throws, `attemptTokenRefresh()` is a no-op for desync (refresh keeps the same ALE key), and the user sees only the generic channel failure. Root cause (wire-proven): GoFrame `Response.WriteStatus(4xx)` writes the literal status text ("Bad Request") into the response buffer before the ALE envelope, shifting the client IV slice and failing AES-GCM auth on every encrypted 4xx | IMPLEMENTED | MC-TXN-CROSSCCY-ATTEMPT / MC-TXN-SAMEACCT-TRANSFER in run 2026-09-08 (`multicurrency-browser-runs.md`); fix-run record appended there (real messages verified over wire and in browser toasts) | `9ef54b53` (on multicurrency baseline `48fe2874`) |
| DEF-029 | P1 | Settings/currency management | Base-currency switch (double-click confirm flow) fails with a generic `Failed to update base currency` toast; `users.main_currency` is not modified. Reproduced 2/2 via UI automation; no `POST /v1/user/update-profile` trace in the available server logs, and because of DEF-032 the access-log gap means it cannot be established whether the request reached the API — First UAT coverage of this path. Root cause (wire-proven after DEF-028 made error messages readable): `BetaScopeMiddleware` deferred **all** `/v1/user/update-*` paths with an ALE 404 "feature unavailable in beta" on production runtimes, so update-profile — the endpoint carrying the base-currency switch — was blocked for every user before reaching its handler | IMPLEMENTED | MC-BASE-SWITCH in run 2026-09-08 (`multicurrency-browser-runs.md`); fix-run record appended there (wire + browser round-trip CNY↔USD, dashboard aggregates USD) | `6a4346e` (web payload tests: gaap-web `a286655`) |
| DEF-030 | P2 | Transaction/multi-currency UX | Transfer mode allows selecting asset accounts of different currencies; the server rejects by design (`account currency mismatch`, both legs must share one currency for **every** transaction type, not just transfers) but there is no client-side prevention or actionable message — combined with DEF-028 the user only sees a generic channel error. Suggest filtering transfer destinations to same-currency assets or an inline hint | IMPLEMENTED (filtering approach; both dropdowns, symmetric either order) | MC-TXN-CROSSCCY-ATTEMPT in run 2026-09-08 (`multicurrency-browser-runs.md`); fix-run record appended there (4 browser gates: EUR card hidden from USD source and vice versa, same-currency control commit still succeeds) | `ef77043` (gaap-web, on baseline `a286655`) |
| DEF-031 | P2 | Settings/accessibility & automation | The "Currency Management" settings sub-nav row and the sidebar avatar are `div` + `onClick` with no `role`/`tabindex`/keyboard support; invisible to DOM automation and keyboard-only users. UAT required a manual mouse click to enter CurrencySettings (the Dashboard trend "Select Accounts" popover has the same visibility issue) | IMPLEMENTED (all four settings rows + sidebar shortcut: role/tabIndex/Enter+Space + aria-labels, styling unchanged; "Select Accounts" verified already a native `<Button>` — no code change) | Manual entry step in run 2026-09-08 (`multicurrency-browser-runs.md`); fix-run record appended there (Tab x12 → Enter enters Currency Management; sidebar shortcut Enter opens Profile settings; Select Accounts menu opens on Enter) | `6349bcd` (gaap-web, on baseline `ef77043`) |
| DEF-032 | P2 | UAT env/observability | Two-part logging problem that obscured defect triage: (1) `logs/api.log` is a HOST-side dev log — multiple local gaap-api dev runs (`./main \| tee logs/api.log`) flood it with RabbitMQ reconnect spam against `127.0.0.1:5772` (dev `.env`; nothing listens on 5772 on the host) while the UAT container is actually healthy (correct compose env rabbitmq:5672, broker log shows a normal AMQP connection since boot); triage initially mistook this file for container output; (2) the production image logs only to stdout with no persistent file adapter, and after ~16:08 +08 (~24 h 45 m uptime) `docker compose logs gaap-api` went completely silent — zero lines through 15+ min of live traffic — while healthchecks stayed green. Root cause (part 2): ghttp access logging was never enabled (`accessLogEnabled` defaults false), so with `logger.level: warning` a quiet day produced no request-level stdout lines at all; root cause (part 1): the canonical dev middleware stack (`docker-middleware-compose.yml`, broker published on 5772 per root `.env`) simply wasn't running, and three orphaned host pipelines shared one log filename with container evidence | IMPLEMENTED (both parts; no ALE/protocol changes. Part 1: killed the 3 stale dev writers, started the dev middleware brokers, `start-dev.sh` now tees to distinct `logs/gaap-api-dev.log`, uses a POSIX air build cmd on macOS and warns when the broker is unreachable before booting; fresh dev run logs `RabbitMQ connected successfully` with zero reconnect spam. Part 2: `config.prod.yaml` enables `server.accessLogEnabled` + day-file logging at `/app/logs` (access lines use level-agnostic `Print`, so they can never be filtered out of stdout), image pre-creates gaap-owned `/app/logs`, compose mounts the `api_logs` volume) | Log forensics in run 2026-09-08; fix-run record appended there (dev: connected, spam-free, new filename; UAT: per-request access lines on docker logs + `/app/logs/access-YYYYMMDD.log` surviving container restart with history intact; reconcile green 83/38) | `b944496` (gaap-api config.prod.yaml + Dockerfile; parent `start-dev.sh` + `docker-compose.uat.yml` in the pointer-sync commit) |

`IMPLEMENTED` only means the code remediation is complete; it does not equal a UAT `PASS`. The 2026-08-13 batch
`UAT-20260813-BETA-RC-01` completed protocol, browser, ALE attack, dependency recovery and read-only reconciliation runs; all Beta-scope defects have been retested. The final reconciliation checked 142 accounts and 62 transactions with no discrepancies or integrity anomalies.
VPS containers, migrations, consumers, restart recovery and empty-DB reconciliation passed. Cloudflare DNS/HTTPS, real Turnstile, origin direct connection and the production business smoke all passed on 2026-08-14; this round added three new non-blocking P2 frontend issues. The release owner recorded production backups, an independent restore drill and daily backups as WAIVED / ACCEPTED RISK at 12:15 CST on 2026-08-14; the final conclusion was updated to GO (invite-only Beta).
